import { Loc } from '../../../core/loc'

import { ORIGIN } from '../../../objects/origin'

import { Cave } from '../../cave'

export function placeGold(chunk: Cave, pt: Loc, depth: number, origin: ORIGIN) {}
