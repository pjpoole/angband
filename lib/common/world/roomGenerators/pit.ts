import { Loc } from '../../core/loc'
import { Cave } from '../cave'
import { Dungeon } from '../dungeon'

export function build(
  dungeon: Dungeon,
  chunk: Cave,
  center: Loc,
  rating: number,
): boolean {
  return false
}
