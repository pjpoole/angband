import { Dungeon } from '../dungeon'
import { Cave } from '../cave'

export function build(
  dungeon: Dungeon,
  chunk: Cave,
  x: number,
  y: number,
  rating: number,
): boolean {
  return false
}
